import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:pokecon/ui/pages/basic_page.dart';

class DomainOverviewPage extends StatelessWidget {
  const DomainOverviewPage({super.key});

  @override
  Widget build(BuildContext context) => const BasicPage(
        title: 'Domain overview',
        child: SizedBox(),
      );
}
